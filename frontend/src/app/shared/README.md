# Shared layer

`shared/` está reservado para componentes/directivas/pipes reutilizables sin estado de negocio específico.

Regla: `shared` puede usarse desde cualquier feature, pero no conoce reglas de dominio.
